# Remote Ticket publication evidence

This directory contains the reproducible final-state constructor and grader for
the GitHub/GitLab remote review-pending and durable local-staging contracts.

Run the deterministic harness with:

```sh
bash tests/ultra-remote-ticket-publication-eval.sh
```
