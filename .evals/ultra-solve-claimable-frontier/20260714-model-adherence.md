# Claimable-Frontier Model-Adherence Run

Date: 2026-07-14

## Scope And Settings

- Treatment commit: `b44151fac9c20d5c3861e23063b0e13057454e8a`.
- Runtime: three fresh Codex subagent sessions, one per isolated fixture.
- Model and reasoning settings: the subagent service used its default model and
  did not expose a model identifier or reasoning setting to the evaluator; this
  record does not infer either value.
- Fixture root: `/tmp/ultra-frontier-eval-20260714` during execution.
- Constructor and grader: committed beside this record under `scripts/`.
- Evidence type: real single-runtime model adherence, graded from final Ticket
  state plus an eval-only audit written only after successful adapter mutations.
  This is not multi-model evidence.

## Prompt Material

Each fresh session was instructed to work only in its scenario repository, read
`EVAL_PROMPT.md`, `AGENTS.md`, `skills/engineering/ultra/SKILL.md`, and
`skills/engineering/ultra/solve.md`, execute the embedded prompt exactly, avoid
editing expectations or runtime files, and leave final filesystem state for
grading.

The exact scenario prompts are generated by `scripts/prepare-fixture.py`:

- Explicit: evaluate the explicit set `A, B, P`, stop after the initial frontier
  generation, and use only the configured tracker wrapper. `B` begins blocked
  by `A`; `P` begins `review-pending`.
- All: repeatedly discover, Claim with the current snapshot, complete, and
  re-read the branching graph `A -> B,C -> D` until the frontier is empty.
- Unsafe: attempt discovery through the configured adapter whose Claim value is
  missing, make no Ticket mutation, and record only the refusal marker.

The wrapper delegates frontier and Claim decisions to the treatment's bundled
adapter. It records successful Claim/completion transitions for grading and
does not implement a second graph.

## Grading

Before the model runs, all untouched fixtures failed their graders. After the
fresh sessions:

| Scenario | Final-state evidence | Result |
| --- | --- | --- |
| Explicit initial frontier | Audit was `claim A`, `complete A`; `A` completed with no active Claim; blocked `B` stayed `ready-for-agent`; provisional `P` stayed `review-pending`. | Pass |
| Iterative `--all` | Audit was `A`, then `B`, `C`, then `D`, with each Claim followed by completion; all Tickets completed with Claims released. | Pass |
| Unsafe contract | Adapter exited nonzero because `Claim value` was absent; no Claim audit or Ticket mutation existed; refusal marker matched the expected category. | Pass |

Exact audit evidence:

```jsonl
01-explicit: {"event": "claim", "ticket": "A"}
01-explicit: {"event": "complete", "ticket": "A"}
02-all: {"event": "claim", "ticket": "A"}
02-all: {"event": "complete", "ticket": "A"}
02-all: {"event": "claim", "ticket": "B"}
02-all: {"event": "complete", "ticket": "B"}
02-all: {"event": "claim", "ticket": "C"}
02-all: {"event": "complete", "ticket": "C"}
02-all: {"event": "claim", "ticket": "D"}
02-all: {"event": "complete", "ticket": "D"}
03-unsafe: {"result": "unsafe-contract-refused"}
```

The grader passed all three repositories with:

```text
PASS explicit
PASS all
PASS unsafe
```

## Interpretation

This run supports the changed selection and Claim behavior: explicit selection
does not advance beyond its initial frontier, `--all` advances only after
completed blockers, provisional Tickets remain excluded, and unsafe Claim
configuration fails closed. Deterministic fixtures separately cover cycles,
missing targets, missing blocker metadata, stale snapshots, Claim conflicts,
both Local Markdown representations, and promoted-publication gating.

The model scenarios intentionally isolate discovery and Claim behavior. They do
not re-evaluate the unchanged worktree, merge, cleanup, or outcome-receipt state
machines, which remain covered by their existing deterministic and model evals.
