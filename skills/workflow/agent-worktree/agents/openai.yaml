interface:
  display_name: "Agent Worktree"
  short_description: "Prepare Agent-ready git worktrees"
  default_prompt: "Use $agent-worktree to create, verify, repair, or remove an Agent-ready git worktree with local payload."
policy:
  allow_implicit_invocation: true
