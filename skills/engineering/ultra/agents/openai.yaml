interface:
  display_name: "Ultra"
  short_description: "Wrap skills with exploration and review"
  default_prompt: "Use $ultra to run an agent skill with adaptive pre-exploration and post-review, or use /ultra solve to execute AFK-ready Tickets through meaningful candidate or recovery outcome receipts."
policy:
  allow_implicit_invocation: true
