interface:
  display_name: "Solve Records"
  short_description: "Manage local Attempt Receipts"
  default_prompt: "Use $solve-records to inspect local Attempt Receipts, safely advance eligible candidates, or explain recovery next actions."
policy:
  allow_implicit_invocation: false
