# Disclose Agent Brief Contract

The ticket-shaping wrapper should keep its body thin and point to the Agent Brief reference when it publishes ready-for-agent tickets. The reference owns the optional, non-duplicative `Constraints`, `Validation`, and `Hints` delta; `/ultra solve` treats the brief as subordinate input rather than a schema gate.
