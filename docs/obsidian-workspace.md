# Obsidian Tracker Workspace

Use [setup-obsidian-workspace](../skills/in-progress/setup-obsidian-workspace/SKILL.md)
for first setup, binding changes, or incomplete-setup repair.

The [workspace contract](../skills/in-progress/setup-obsidian-workspace/references/workspace.md)
owns configuration, dependencies, source navigation, refresh guarantees and parity.

The repository compatibility command remains:

```bash
python3 scripts/obsidian-workspace.py --config /absolute/path/workspace.json
```
